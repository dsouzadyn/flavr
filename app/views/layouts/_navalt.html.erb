<div class="ui secondary pointing menu">
  <a href="/" class="active item">
    flavr.
  </a>
  <div class="right menu">
    <% if logged_in? %>
      <%= link_to "Sign Out", logout_path, :class => "ui item" %>
    <% else %>
      <%= link_to "Login", login_path, :class => "ui item" %>
    <% end %>
  </div>
</div>
